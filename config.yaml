apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
data:   # （1）設定情報を含むデータ
  default.conf.template: |
    server {
        location / {
            return 200 'Hello $MY_ENV';
            add_header Content-Type text/plain;
        }
    }
